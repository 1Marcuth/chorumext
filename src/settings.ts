import { ISettings } from "./interfaces/settings"

const settings: ISettings = {
    server: {
        host: "localhost",
        port: 8000,
        environment: "dev"
    }
}

export default settings